# Journal des erreurs (rempli automatiquement)

<!-- Chaque échec d'outil pendant une session est ajouté ici par le hook
     `memoire-erreur.sh` : date · outil · message (tronqué) · session.
     Ne pas rédiger à la main. Le journal est plafonné (rotation automatique).
     La passe de dreaming l'analyse pour repérer les erreurs récurrentes,
     puis marque [traité] les lignes converties en leçons. -->
